# 明日方舟寻访公告 API 文档

## 脚本信息

- **文件名**: `ArknightsBanner.py`
- **定时任务**: `0 10 * * *` (每天10:00执行)
- **功能**: 监控明日方舟官网限时寻访（卡池）公告，提取每期卡池数据并在更新时推送通知

## 功能说明

该脚本会：
1. 扫描首页(LATEST)全部公告 + ACTIVITY 兜底前几页，收集所有新 cid 公告
2. 对每个新公告抓取详情正文，**按内容判断**是否卡池（不靠标题/摘要预筛）
3. 解析卡池结构化数据：寻访类型 / 名称 / 活动时间 / UP干员 / 出率 / 保底规则 / 兑换所
4. 有卡池封面图则推送图片(Telegram)+文字描述(全通道)，无图则纯文字
5. 与本地状态文件比对，仅在有**新卡池公告**时推送通知

## 卡池公告识别（按内容）

不依赖标题/摘要预筛，直接看详情正文是否含卡池数据特征词：

| 判断词 | 说明 |
|:---|:---|
| `出现率上升` / `占6★出率` / `占5★出率` / `占4★出率` / `6★出率` / `5★出率` / `4★出率` / `概率提升` | 正文含任一即判定为卡池公告 |

> 这样可抓到标题无"寻访"但内容是卡池的公告，例如：
> - `[活动预告]主题曲第十二章预热...` 正文含【前路回响】限时寻访 + UP干员 + 出率
> - `[活动预告]故事集「丛林症结」...` 正文含【确定性混沌】限时寻访（标准寻访）+ 谬因/佩德洛/钼铅 UP
>
> 卡池预告（即将开启的寻访）也算并优先推送。

## 扫描策略（增量）

- **首次运行**：扫首页(LATEST)全部 + ACTIVITY 前 5 页，记录所有 cid 为已知，按内容定位最新卡池并推送
- **日常运行**：LATEST 全部 + ACTIVITY 增量（遇整页已知即停），对新 cid 抓详情按内容判断，仅推送新增卡池
- `known_cids` 记录所有扫描到的公告 cid（含非卡池），用作增量停止基线

## 官网与接口

### 公告官网

- **公告列表页**: `https://ak.hypergryph.com/news`
- **公告详情页**: `https://ak.hypergryph.com/news/{cid}`

### 列表接口（逆向所得）

**URL:** `GET https://ak.hypergryph.com/api/news?category={category}&page={page}`

**参数:**

| 参数 | 说明 |
|:---|:---|
| `category` | `LATEST`(最新) / `ANNOUNCEMENT`(公告) / `ACTIVITY`(活动) / `NEWS`(新闻) |
| `page` | 页码，从 1 开始，每页 6 条 |

**响应:**

```json
{
  "code": 0,
  "data": {
    "list": [
      {
        "cid": "8588",
        "tab": "1",
        "sticky": false,
        "title": "[狩猎凯旋干员寻访]【砺火成锋】限时寻访即将复刻开启",
        "author": "【明日方舟】运营组",
        "displayTime": 1781254800,
        "cover": "",
        "extraCover": "",
        "brief": "活动时间：06月15日 16:00 - 06月29日 03:59..."
      }
    ],
    "total": 230,
    "end": false
  }
}
```

| 字段 | 说明 |
|:---|:---|
| `cid` | 公告 ID，对应详情页 URL `/news/{cid}` |
| `tab` | 分类：`0`=公告 `1`=活动 `2`=新闻 |
| `title` | 公告标题，`[寻访类型]【寻访名称】` 格式 |
| `displayTime` | 发布时间 Unix 时间戳（秒） |
| `brief` | 摘要（截断） |
| `total` | 该分类公告总数 |
| `end` | 是否末页 |

### 详情接口

**URL:** `GET https://ak.hypergryph.com/news/{cid}`

返回 Next.js SSR HTML，正文为 `<p>` 段落。脚本通过以下规则提取正文：

1. 移除所有 `<script>` 块（含 RSC flight data，避免内容重复）
2. 以日期标记 `\d{4} // \d{2} / \d{2}` 作为正文起点
3. 截断到健康提示页脚「本网络游戏适合...」前
4. 提取所有 `<p>...</p>` 段落纯文本
5. 提取 `data-width="1560"` 的 `<img>` 作为卡池封面

## 卡池数据提取规则

适配两种公告格式：独立寻访（`★★★★★★：麒麟R夜刀（占6★出率的50%）`）与活动附带寻访（`★★★★★★（6★出率：2%）：琴柳 / 号角`）。

| 字段 | 提取规则 |
|:---|:---|
| 寻访类型 | 优先正文 `◆...寻访为【xxx】`，回退标题 `\[xxx\]` 前缀 |
| 寻访名称 | 优先正文 `活动期间【xxx】...寻访开启`，回退标题 `【xxx】` |
| 活动时间 | 定位寻访说明段（含"出现率上升/以下干员/活动期间...寻访"），取其前面最近的 `活动时间：` |
| 6★ UP干员 | `^★{6}` 开头、含"出率"、不含"寻访数据契约"的段落（排除新增干员列表） |
| 5★ UP干员 | `^★{5}` 开头、含"出率"、不含"寻访数据契约"的段落 |
| 保底与规则 | `◆` 段且含"寻访/出率/保底/必定/累计/六星/五星"，排除"时装/回顾展/信赖获取/合成玉"等杂物 |
| 兑换所 | 含「寻访数据契约」且为交换所/可兑换干员的段落 |
| 卡池封面 | `data-width="1560"` 的 `<img>` src，有则随通知推送图片 |

## 状态文件

### config/.arknights_banner_state.json

```json
{
  "known_cids": ["2825", "7377", "8588"],
  "notified_cids": ["2825", "7377", "8588"],
  "last_run": "2026-07-07 22:10:04"
}
```

- `known_cids`: 已扫描到的所有公告 cid（含非卡池，用于增量停止基线）
- `notified_cids`: 已推送通知的 cid
- 首次运行时扫描全部页并记录所有 cid，仅推送最新一期；后续增量扫描遇已知即停，仅推送新增卡池

## 输出报告格式

```
🎰 明日方舟 新卡池公告

🏷️ 寻访类型: 标准寻访
📛 寻访名称: 确定性混沌
📅 活动时间: 07月10日 12:00 - 07月24日 03:59
📰 发布时间: 2026-07-05 11:00
🖼️ 卡池封面: 见推送图片

⭐ 6★ UP干员（出现率上升）
  ★★★★★★：谬因（占6★出率的50%）

⭐ 5★ UP干员（出现率上升）
  ★★★★★： 佩德洛 / 钼铅（占5★出率的50%）

📋 保底与规则
  ◆本次活动【确定性混沌】寻访为【标准寻访】，如果连续150次没有获得...
  ◆[确定性混沌寻访凭证]仅可用于【确定性混沌】寻访...

🔗 公告链接: https://ak.hypergryph.com/news/9686
──────────────────
🕒 推送时间: 2026-07-07 23:01:59
```

> 卡池封面图通过 `notifier.send_photos` 推送：文字描述发全部通知通道，图片发 Telegram（支持 URL 直传）。
